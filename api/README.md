# Prérequis

Installer docker

# Comment lancer l'api ?

`cd api`

Copier le contenu _.env.exemple_ dans un nouveau fichier _.env_

Si besoin, remplacer le contenu des variables

`docker compose up --build -d`

# Comment couper l'api ?

`docker compose down`
